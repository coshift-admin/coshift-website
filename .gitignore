# Dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*

# Testing
/coverage

# Next.js
/.next/
/out/

# Production
/build

# Misc
.DS_Store
*.pem

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Env files (DO NOT COMMIT)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
!.env.example

# Vercel
.vercel

# Typescript
*.tsbuildinfo
next-env.d.ts

# Local tooling
.playwright-mcp/
.local/

# Don't commit secrets or backups
.env
.env.local
docker-compose.yml.backup-*
next.config.ts.backup

# Internal-only: contains unsigned prospect names + infra notes (brief §11 says
# never publish these). Keep it local; never push to a hosted repo.
Coshift_Brand_Identity_Brief.md

# Raw logo dropbox (source assets incl. non-client marks). Processed, cleared
# client logos that the site actually uses live in public/logos/.
/logos/
